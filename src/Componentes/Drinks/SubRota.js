import React from 'react'
import { useParams } from 'react-router-dom'

const SubRota = () => {
    const { SubRota } = useParams()
    const minhaRef = React.useRef(null)

    const passaPorCima = (e) => {
        
    }
    return (
        <div>
                
        </div>
    )
}

export default SubRota
